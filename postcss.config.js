/*
*   This file is used to configure the PostCSS plugins
*   PostCSS is a tool for transforming CSS with JavaScript.
*   tailwindcss is used to add Tailwind CSS to your project.
*   autoprefixer is used to parse CSS and add vendor prefixes to CSS rules using values from Can I Use.

*/
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
